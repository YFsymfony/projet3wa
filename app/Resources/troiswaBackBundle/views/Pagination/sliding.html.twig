{# default Sliding pagination control implementation #}

{% if pageCount > 1 %}
    <div class="pagination">
        {% if first is defined and current != first %}
            <span class="first">
            <a class="btn btn-primary" href="{{ path(route, query|merge({(pageParameterName): first})) }}"><i class="fa fa-arrow-circle-left"></i> <i class="fa fa-arrow-circle-left"></i></a>
        </span>
        {% endif %}

        {% if previous is defined %}
            <span class="previous">
            <a class="btn btn-primary" href="{{ path(route, query|merge({(pageParameterName): previous})) }}"><i class="fa fa-arrow-circle-left"></i></a>
        </span>
        {% endif %}

        {% for page in pagesInRange %}
            {% if page != current %}
                <span class="page">
                <a class="btn btn-primary" href="{{ path(route, query|merge({(pageParameterName): page})) }}">{{ page }}</a>
            </span>
            {% else %}
                <span class="current">{{ page }}</span>
            {% endif %}

        {% endfor %}

        {% if next is defined %}
            <span class="next">
            <a class="btn btn-primary" href="{{ path(route, query|merge({(pageParameterName): next})) }}"><i class="fa fa-arrow-circle-right"></i></a>
        </span>
        {% endif %}

        {% if last is defined and current != last %}
            <span class="last">
            <a class="btn btn-primary" href="{{ path(route, query|merge({(pageParameterName): last})) }}"><i class="fa fa-arrow-circle-right"></i> <i class="fa fa-arrow-circle-right"></i></a>
        </span>
        {% endif %}
    </div>
{% endif %}



{# fichier de base ou copier / coller les éléments a surcharger :
 ...../vendor/symfony/symfony/src/Symfony/Bridge/Twig/Resources/views/Form/form_div_layout.html.twig#}

{# On a surchargé l'affichage des erreurs de base #}

{%- block form_errors -%}
    {%- if errors|length > 0 -%}
        <ul class="alert alert-danger list-unstyled">
            {%- for error in errors -%}
                <li>{{ error.message }}</li>
            {%- endfor -%}
        </ul>
    {%- endif -%}
{%- endblock form_errors -%}

